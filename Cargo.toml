[package]
name = "octomind"
version = "0.16.0"
edition = "2021"
authors = ["Muvon Un Limited <contact@muvon.io>"]
license = "Apache-2.0"
description = "Session-based AI development assistant with conversational codebase interaction, multimodal vision support, built-in MCP tools, and multi-provider AI integration"
homepage = "https://octomind.muvon.io"
repository = "https://github.com/muvon/octomind"
documentation = "https://octomind.muvon.io"
keywords = ["ai", "development", "assistant", "productivity", "conversational"]
categories = ["development-tools", "command-line-utilities"]
readme = "README.md"
rust-version = "1.82"
exclude = [
    "tests/fixtures/*",
    "examples/*",
    ".github/*",
    "docs/*",
    "scripts/*",
    "*.md",
    "install.sh"
]

# Enable static linking
[profile.release]
lto = true        # Enable Link Time Optimization
codegen-units = 1 # Reduce parallel code generation units for better optimization
panic = "abort"  # Abort on panic for smaller binary size
strip = true      # Strip symbols from binary

[dependencies]
# octolib = { path = "../octolib", default-features = false }
octolib = { version = "0.6.0", default-features = false }

tokio = { version = "1.49.0", features = ["rt-multi-thread", "macros", "time", "process", "fs", "sync", "signal"] }
crossterm = "0.29.0"
indicatif = "0.18.3"
parking_lot = "0.12.5"
chrono = { version = "0.4.43", features = ["serde"] }
serde = { version = "1.0.228", features = ["derive"] }
uuid = { version = "1.20.0", features = ["v4"] }
tiktoken-rs = "0.9.1" # Token counter
reqwest = { version = "0.13.1", features = ["json", "rustls", "gzip"], default-features = false }
anyhow = "1.0.100"
serde_json = "1.0.149"
clap = { version = "4.5.54", features = ["derive"] }
clap_complete = "4.5.65"
toml = "0.9.11"
lazy_static = "1.5.0"
tokio-util = { version = "0.7", features = ["rt"] }
rustyline = { version = "17.0.2", features = ["custom-bindings"] }
regex = "1.12.2"
colored = "3.1.1"
async-trait = "0.1.89"
termimad = "0.34.1"
syntect = "5.3.0"
html5ever = "0.36.1"
markup5ever_rcdom = "0.36.0"
url = "2.5.8"
dirs = "6.0.0"
glob = "0.3.3"
ignore = "0.4.25"
futures = "0.3.31"
dotenvy = "0.15.7"
arboard = "3.6.1"
image = { version = "0.25.6", default-features = false, features = ["png", "jpeg", "gif", "webp", "bmp"] }
viuer = "0.11.0"
base64 = "0.22"
urlencoding = "2.1.3"
humantime-serde = "1.1.1"
tokio-tungstenite = "0.28.0"
futures-util = "0.3"

# OAuth 2.1 + PKCE authentication
oauth2 = { version = "5.0", features = ["reqwest"] }
keyring = { version = "3" }
sha2 = "0.10"
rand = "0.8"
serde_urlencoded = "0.7"
thiserror = "2.0"
open = "5"
tracing = { version = "0.1", default-features = false }

[dev-dependencies]
tempfile = "3.24.0"
serial_test = "3.3.1"

[profile.dev]
opt-level = 1          # Basic optimizations without slowing compilation too much
debug = true           # Keep debug symbols for backtraces
debug-assertions = true # Keep runtime checks
overflow-checks = true  # Keep overflow checks
lto = false            # Disable Link Time Optimization to speed up builds
codegen-units = 256    # Use more codegen units for faster parallel compilation
incremental = true     # Enable incremental compilation
rpath = false          # Disable runtime search path
